name: Build Haven Desktop

on:
  pull_request:
  push:
    tags:
      - 'v*'          # Trigger on version tags like v1.0.0
  workflow_dispatch:    # Manual trigger from GitHub UI

permissions:
  contents: write      # Needed to create releases

jobs:
  # ─── Windows Build ──────────────────────────────────────
  build-windows:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install GStreamer SDK
        shell: pwsh
        run: |
          choco install gstreamer gstreamer-devel --version=1.26.9 -y
          "GSTREAMER_1_0_ROOT_MSVC_X86_64=C:\gstreamer\1.0\msvc_x86_64" | Out-File -FilePath $env:GITHUB_ENV -Append
          "C:\gstreamer\1.0\msvc_x86_64\bin" | Out-File -FilePath $env:GITHUB_PATH -Append

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build native audio addon
        run: npm run build:native

      - name: Stage native media runtime
        run: npm run stage:native-runtime

      - name: Probe staged native media runtime
        shell: pwsh
        run: |
          $env:PATH = "$PWD\native\runtime\gstreamer\bin;$env:PATH"
          $env:GST_PLUGIN_PATH_1_0 = "$PWD\native\runtime\gstreamer\plugins"
          $env:GST_PLUGIN_SYSTEM_PATH_1_0 = $env:GST_PLUGIN_PATH_1_0
          $env:GST_PLUGIN_SCANNER_1_0 = "$PWD\native\runtime\gstreamer\libexec\gst-plugin-scanner.exe"
          $probeOutput = .\native\build\Release\haven_screen_share.exe --probe | Out-String
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          node -e "const p=JSON.parse(process.argv[1]); if(p.protocolVersion!==3||p.supported!==true||p.components?.capture!==true||p.components?.encoder!==true||p.components?.audio!==true||p.components?.transport!==true) process.exit(1)" $probeOutput.Trim()
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          npm run smoke:native-screen
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $env:HAVEN_NATIVE_FORCE_SOFTWARE = "1"
          npm run smoke:native-screen
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      # electron-builder 26 uploads the installer and its blockmap in parallel,
      # and when no release exists yet each upload creates one, so a tag ended
      # up with two releases (1.4.29). Creating the release first gives both
      # jobs one place to attach to. releaseType is "release" in
      # electron-builder.yml, which matches a published release, so the
      # publisher reuses it instead of refusing it.
      - name: Create the GitHub release before publishing
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        run: |
          gh release view "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1 \
            || gh release create "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" --title "${GITHUB_REF_NAME#v}" --notes "Release notes to follow."
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Publish Windows installer
        run: npx electron-builder --win --publish ${{ startsWith(github.ref, 'refs/tags/v') && 'always' || 'never' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: haven-desktop-windows
          path: |
            dist/*.exe
          retention-days: 30

  # ─── Linux Build ────────────────────────────────────────
  build-linux:
    needs: [build-windows]
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install native media development packages
        run: sudo apt-get update && sudo apt-get install -y libpulse-dev libx11-dev libxtst-dev libxinerama-dev libxt-dev libxrandr-dev libxfixes-dev libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev libgstreamer-plugins-bad1.0-dev gstreamer1.0-plugins-base gstreamer1.0-plugins-good gstreamer1.0-plugins-bad gstreamer1.0-plugins-ugly gstreamer1.0-nice gstreamer1.0-pipewire gstreamer1.0-vaapi

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build native audio addon
        run: npm run build:native

      - name: Stage native media runtime
        run: npm run stage:native-runtime

      - name: Probe staged native media runtime
        run: |
          export LD_LIBRARY_PATH="$PWD/native/runtime/gstreamer/lib"
          export GST_PLUGIN_PATH_1_0="$PWD/native/runtime/gstreamer/plugins"
          export GST_PLUGIN_SYSTEM_PATH_1_0="$PWD/native/runtime/gstreamer/plugins"
          export GST_PLUGIN_SCANNER_1_0="$PWD/native/runtime/gstreamer/libexec/gst-plugin-scanner"
          probe_output=$(native/build/Release/haven_screen_share --probe)
          node -e "const p=JSON.parse(process.argv[1]); if(p.protocolVersion!==3||p.supported!==true||p.components?.capture!==true||p.components?.encoder!==true||p.components?.audio!==true||p.components?.transport!==true) process.exit(1)" "$probe_output"
          npm run smoke:native-screen
          HAVEN_NATIVE_FORCE_SOFTWARE=1 npm run smoke:native-screen

      - name: Build & Publish Linux packages
        run: npx electron-builder --linux --publish ${{ startsWith(github.ref, 'refs/tags/v') && 'always' || 'never' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v4
        with:
          name: haven-desktop-linux
          path: |
            dist/*.AppImage
            dist/*.deb
          retention-days: 30
